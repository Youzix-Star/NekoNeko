<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Color theme overlay styles.
      These are empty shell styles; the actual color overrides are applied
      programmatically by ColorThemeManager.applyTheme().
      These style resources exist as placeholders for future XML-based theme
      switching if needed.
    -->
</resources>
